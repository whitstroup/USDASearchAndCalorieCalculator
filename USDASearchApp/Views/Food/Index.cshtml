@model List<Food>



    @using (Html.BeginForm("Search", "Food", FormMethod.Get))
{
<p>
    Find by name: @Html.TextBox("searchString")
    <input type="submit" value="Search" />
</p>}

    <table class="table table-striped table-dark pagination-lg">
    <thead class="thead-dark">
        <tr>
            <th>Name</th>
            <th>Brand</th>
            <th>Descrption</th>
            <th>
                <a asp-controller="cart" asp-action="index" >View Cart</a>
            </th>

        </tr>
    </thead>

    @foreach (var food in Model)
    {
<tr>
    <td><a href=/Food/ViewNutrient/@food.fdcId>Details</a></td>
    <td>@food.description</td>
    <td>@food.brandOwner</td>
    <td>@food.additionalDescriptions</td>
    <td align="center">
        <a asp-controller="cart" asp-action="buy" asp-route-id="@food.fdcId">AddToCart</a>
    </td>
   
</tr>
}

</table>